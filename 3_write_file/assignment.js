/**
    Write a script that will read a json file, that contains a number,
    increment it, and write the changes back to the file.

    // the json can look like this:
    {
        "number": 5
    }
 */
